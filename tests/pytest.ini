[pytest]

# Sets this flag to the default 'pytest each time'. ie, a report will always be generated
addopts = --html=reports/report.html --self-contained-html

# Pytest-html option: generate report as each test finishes
generate_report_on_test = True